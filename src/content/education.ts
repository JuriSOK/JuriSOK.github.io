import type { Education } from '../types/content'

/**
 * Formation.
 *
 * Comme pour les expériences, seuls les faits fournis figurent ici. Les
 * formations antérieures au Master seront ajoutées quand elles seront connues.
 *
 * À REMPLIR — `period`, `school`, `fields`, `highlights`, et les diplômes
 * précédents.
 */
export const education: readonly Education[] = [
  {
    id: 'master-miage',
    degree: 'Master MIAGE',
  },
]
